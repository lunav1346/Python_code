a = b = c = d = 10 

#값은 해당 변수(주소값이 가르치는 데이터 저장소)에 저장되는 데이터 값

# a, b, c 모두 d의 주소값을 가지고 있는 것인가?
# 그렇다면, d 하나만 바꿔도 a, b, c의 값까지 전부 바뀌는 것인가?

print(a, b, c, d)
e, f, g, h = 10, 10, 10, 10
print(e, f, g, h)

# = : is assigned to
# = 할당 연산자

#del(변수) 하면 변수 삭제
